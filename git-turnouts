#!/bin/bash

# Git Turnouts - Your Git branch switching yard
# Manage Git worktrees with intelligent GitHub Pull Request integration

set -e

VERSION="1.3.0"

# ============================================================================
# SHARED HELPER FUNCTIONS
# ============================================================================

# Function to get PR branch by title (prefers exact match, falls back to partial)
get_pr_branch_by_title() {
  local search_term="$1"
  local exact_match="$2"

  # Fetch open PRs
  local pr_list=$(gh pr list --json number,title,headRefName 2>/dev/null)
  if [ -z "$pr_list" ] || [ "$pr_list" = "[]" ]; then
    return 1
  fi

  # Search using jq: prefer exact match, fallback to partial if exact_match=false
  echo "$pr_list" | jq -r --arg term "$search_term" --arg exact "$exact_match" '
    ([.[] | select(.title == $term)] | .[0]) as $exact_res |
    if $exact_res then
      $exact_res
    elif $exact == "false" then
      ([.[] | select(.title | contains($term))] | .[0])
    else
      empty
    end | .headRefName // ""
  '
}

# Function to get PR details by PR number
get_pr_details_by_number() {
  local pr_number="$1"

  # Get PR details including state and merge information
  gh pr view "$pr_number" --json headRefName,state,mergedAt,baseRefName 2>/dev/null
}

# Function to get PR branch by PR number
get_pr_branch_by_number() {
  local pr_number="$1"

  # Get PR details by number
  gh pr view "$pr_number" --json headRefName 2>/dev/null | \
    jq -r '.headRefName' 2>/dev/null
}

# Function to check if a string is a PR number (all digits)
is_pr_number() {
  local input="$1"
  [[ "$input" =~ ^[0-9]+$ ]]
}

# Function to check if a string is quoted and strip quotes
check_and_strip_quotes() {
  local input="$1"
  # Check if string starts with " or ' and ends with the same quote
  if [[ "$input" =~ ^\"(.*)\"$ ]] || [[ "$input" =~ ^\'(.*)\'$ ]]; then
    echo "${BASH_REMATCH[1]}"
    return 0  # Was quoted
  else
    echo "$input"
    return 1  # Was not quoted
  fi
}

# Get the main worktree path
get_main_worktree() {
  git worktree list | head -n1 | awk '{print $1}'
}

# Get project name from main repository
get_project_name() {
  local main_worktree=$(get_main_worktree)
  basename "$main_worktree"
}

# Get base worktree directory
# Returns the base directory where worktrees should be created
# The project name will be automatically added as a subdirectory
get_base_worktree_dir() {
  local project_name=$(get_project_name)

  # Use hierarchical lookup: project-specific → global → auto-detect
  local base_dir=$(get_config "worktree.base_dir" "" "$project_name")

  if [ -n "$base_dir" ]; then
    echo "$base_dir"
  else
    # Fallback to automatic detection (current behavior)
    local main_worktree=$(get_main_worktree)
    echo "$(dirname "$main_worktree")/worktree"
  fi
}

# Get absolute path (portable alternative to realpath)
# Works on all Unix systems without requiring the realpath command
get_absolute_path() {
  local path="$1"

  # If path is empty, return empty
  if [ -z "$path" ]; then
    echo ""
    return
  fi

  # Expand tilde to home directory
  case "$path" in
    "~/"*)
      path="${HOME}/${path#\~/}"
      ;;
    "~")
      path="$HOME"
      ;;
  esac

  # If path exists as a directory, cd into it and get pwd
  if [ -d "$path" ]; then
    (cd "$path" && pwd)
  # If path doesn't exist yet, resolve the parent directory
  elif [ -e "$path" ]; then
    # Path exists as a file
    local dir=$(dirname "$path")
    local base=$(basename "$path")
    echo "$(cd "$dir" 2>/dev/null && pwd)/$base"
  else
    # Path doesn't exist yet - resolve what we can
    local dir=$(dirname "$path")
    local base=$(basename "$path")
    if [ -d "$dir" ]; then
      echo "$(cd "$dir" && pwd)/$base"
    else
      # Even parent doesn't exist, return as-is
      # This will be created by mkdir -p later
      echo "$path"
    fi
  fi
}

# ============================================================================
# REMOTE BRANCH VERIFICATION FUNCTIONS
# ============================================================================

# Check if a branch exists on the remote
# Arguments: $1 = branch_name
# Returns: 0 if exists, 1 if not
check_remote_branch_exists() {
  local branch="$1"

  # Check if remote tracking ref exists locally
  if git show-ref --verify --quiet "refs/remotes/origin/$branch"; then
    return 0
  else
    return 1
  fi
}

# Check if local branch has unpushed commits
# Arguments: $1 = branch_name
# Sets global: UNPUSHED_COMMIT_COUNT
# Returns: 0 if has unpushed commits, 1 if not or branch deleted
check_unpushed_commits() {
  local branch="$1"

  # Check if remote branch exists
  if ! git show-ref --verify --quiet "refs/remotes/origin/$branch"; then
    UNPUSHED_COMMIT_COUNT=0
    return 1
  fi

  # Count commits ahead of remote
  UNPUSHED_COMMIT_COUNT=$(git rev-list --count "origin/$branch..$branch" 2>/dev/null || echo "0")

  if [ "$UNPUSHED_COMMIT_COUNT" -gt 0 ]; then
    return 0
  else
    return 1
  fi
}

# Check if a branch is hard-protected (main, master, repository default, or configured)
# These branches can NEVER be removed, even with --force
# Arguments: $1 = branch name
# Returns: 0 (true) if hard-protected, 1 (false) if not
is_hard_protected_branch() {
  local branch="$1"

  # Get default branch
  local default_branch
  default_branch=$(git symbolic-ref refs/remotes/origin/HEAD 2>/dev/null | sed 's@^refs/remotes/origin/@@' || echo "main")

  # Hardcoded hard-protected branches: main, master, and the repository's default branch
  for protected in "main" "master" "$default_branch"; do
    if [ "$branch" = "$protected" ]; then
      return 0  # Hard-protected
    fi
  done

  # Check configured hard-protected branches (project-specific → global → default: empty)
  local hard_protected_branches=$(get_config "protection.hard_protected_branches" "")

  # Check against configured hard-protected branches
  for protected in $hard_protected_branches; do
    if [ "$branch" = "$protected" ]; then
      return 0  # Hard-protected
    fi
  done

  return 1  # Not hard-protected
}

# Check if a branch is protected (either hard-protected or user-configured)
# Arguments: $1 = branch name
# Returns: 0 (true) if protected, 1 (false) if not protected
is_protected_branch() {
  local branch="$1"

  # Always include hard-protected branches
  if is_hard_protected_branch "$branch"; then
    return 0
  fi

  # Get configured protected branches (project-specific → global → default: empty)
  local protected_branches=$(get_config "protection.protected_branches" "")

  # Check against configured protected branches
  for protected in $protected_branches; do
    if [ "$branch" = "$protected" ]; then
      return 0  # Protected
    fi
  done

  return 1  # Not protected
}

# Analyze all worktrees for remote branch status
# Arguments: $1 = verbose (true/false)
# Sets globals: WORKTREES_WITH_REMOTE, WORKTREES_WITHOUT_REMOTE, WORKTREES_PROTECTED_STALE, WORKTREE_PATHS
analyze_worktrees_remote_status() {
  local verbose="${1:-false}"

  # Initialize result variables
  WORKTREES_WITH_REMOTE=""
  WORKTREES_WITHOUT_REMOTE=""
  WORKTREES_PROTECTED_STALE=""
  WORKTREE_PATHS=""

  # Get the main worktree path (the repository root)
  local main_worktree_path=$(git rev-parse --show-toplevel)

  # Parse git worktree list
  while IFS= read -r line; do
    # Skip lines without branch info (shouldn't happen with git worktree list)
    if [[ ! "$line" =~ \[([^\]]+)\] ]]; then
      continue
    fi

    local branch="${BASH_REMATCH[1]}"
    local path=$(echo "$line" | awk '{print $1}')

    # Skip the primary/main worktree (the repo itself)
    # Compare paths by resolving both to absolute paths
    local resolved_path=$(cd "$path" 2>/dev/null && pwd || echo "$path")
    local resolved_main=$(cd "$main_worktree_path" 2>/dev/null && pwd || echo "$main_worktree_path")

    if [ "$resolved_path" = "$resolved_main" ]; then
      if [ "$verbose" = "true" ]; then
        echo "⏭️  $branch → skipping main worktree"
      fi
      continue
    fi

    # Store path mapping
    WORKTREE_PATHS="$WORKTREE_PATHS ${branch}:${path}"

    # Check remote status
    if check_remote_branch_exists "$branch"; then
      WORKTREES_WITH_REMOTE="$WORKTREES_WITH_REMOTE $branch"
      if [ "$verbose" = "true" ]; then
        # Check if this active branch is also protected
        if is_protected_branch "$branch"; then
          echo "✅ $branch → origin/$branch exists (PROTECTED)"
        else
          echo "✅ $branch → origin/$branch exists"
        fi
      fi
    else
      # Remote branch deleted - check if protected
      if is_protected_branch "$branch"; then
        WORKTREES_PROTECTED_STALE="$WORKTREES_PROTECTED_STALE $branch"
        if [ "$verbose" = "true" ]; then
          echo "🛡️  $branch → branch deleted from remote (PROTECTED - will not be removed)"
        fi
      else
        WORKTREES_WITHOUT_REMOTE="$WORKTREES_WITHOUT_REMOTE $branch"
        if [ "$verbose" = "true" ]; then
          echo "❌ $branch → branch deleted from remote"
        fi
      fi
    fi
  done < <(git worktree list)

  # Trim leading spaces
  WORKTREES_WITH_REMOTE="${WORKTREES_WITH_REMOTE## }"
  WORKTREES_WITHOUT_REMOTE="${WORKTREES_WITHOUT_REMOTE## }"
  WORKTREES_PROTECTED_STALE="${WORKTREES_PROTECTED_STALE## }"
  WORKTREE_PATHS="${WORKTREE_PATHS## }"
}

# Check for stale entries in the project-specific configuration
# A stale entry is a protected branch that no longer exists locally or on origin
# Arguments: $1 = project name
# Sets global: STALE_CONFIG_ENTRIES
check_stale_config_entries() {
  local project="$1"
  STALE_CONFIG_ENTRIES=""

  if [ -z "$project" ]; then
    return 0
  fi

  # Get project-specific protected branches (skip global to avoid cross-project side effects)
  local protected_branches=""
  for entry in $CONFIG_PROTECTION_PROJECTS; do
    if [[ "$entry" == "$project:"* ]]; then
      protected_branches="${entry#*:}"
      # Convert comma-separated to space-separated
      protected_branches="${protected_branches//,/ }"
      break
    fi
  done
  
  if [ -z "$protected_branches" ]; then
    return 0
  fi

  for branch in $protected_branches; do
    # Skip hard-protected branches (they are always relevant)
    if is_hard_protected_branch "$branch"; then
      continue
    fi

    # Check if branch exists locally or on remote
    if ! git rev-parse --verify "$branch" >/dev/null 2>&1 && \
       ! git rev-parse --verify "origin/$branch" >/dev/null 2>&1; then
      STALE_CONFIG_ENTRIES="$STALE_CONFIG_ENTRIES $branch"
    fi
  done

  # Trim leading spaces
  STALE_CONFIG_ENTRIES="${STALE_CONFIG_ENTRIES## }"
}

# ============================================================================
# CONFIGURATION SYSTEM
# ============================================================================

# Configuration variables (bash 3.2 compatible - no associative arrays)
# All configuration sections now support both global and project-specific settings
# Format: "project1:value1 project2:value2"

# Worktree configuration
CONFIG_WORKTREE_GLOBAL_BASE_DIR=""       # Global base directory (project name added automatically)
CONFIG_WORKTREE_PROJECTS=""              # Space-separated: "project1:path1 project2:path2"
CONFIG_WORKTREE_GLOBAL_COPY_FILES=""     # Space-separated list of global copy files
CONFIG_WORKTREE_PROJECTS_COPY_FILES=""   # Space-separated: "project1:file1,file2 project2:file3"

# Defaults configuration
CONFIG_DEFAULTS_GLOBAL_OPEN_WITH=""  # No default - opening is opt-in
CONFIG_DEFAULTS_PROJECTS=""              # Space-separated: "project1:editor1 project2:editor2"

# Protection configuration
CONFIG_PROTECTION_GLOBAL_BRANCHES="main master"  # Hardcoded defaults (always protected)
CONFIG_PROTECTION_PROJECTS=""            # Space-separated: "project1:dev,staging project2:prod"
CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES=""  # Additional branches that can NEVER be removed
CONFIG_PROTECTION_PROJECTS_HARD=""       # Space-separated: "project1:branch1,branch2"

# Remove configuration
CONFIG_REMOVE_GLOBAL_AUTO_PRUNE="true"   # Hardcoded default
CONFIG_REMOVE_PROJECTS=""                # Space-separated: "project1:false project2:true"

# Parse simple YAML configuration (bash 3.2 compatible)
# Supports hierarchical structure: section.global.key and section.projects[].key
# All sections support both global and project-specific settings
parse_yaml_config() {
  local config_file="$1"
  local section=""              # Level 1: global, projects
  local in_project_item=false   # Are we inside a project list item?
  local in_list=false           # Are we inside a list (copy_files, protected_branches)?
  local list_type=""            # What type of list (copy_files, protected_branches)
  local sub_section=""          # Current sub-section (global, projects)

  # Temporary variables for building project entries
  local current_project_name=""
  local current_base_dir=""
  local current_open_with=""
  local current_auto_prune=""
  local current_copy_files=""
  local current_protected_branches=""
  local current_hard_protected_branches=""

  while IFS= read -r line || [ -n "$line" ]; do
    # Skip comments and empty lines
    [[ "$line" =~ ^[[:space:]]*# ]] && continue
    [[ -z "${line// /}" ]] && continue

    # No indent: Section headers (e.g., "global:", "projects:")
    if [[ "$line" =~ ^([a-z_]+):([[:space:]]*|[[:space:]]+.*)$ ]]; then
      # Save any pending project data before switching sections
      save_project_data

      section="${BASH_REMATCH[1]}"
      in_list=false
      in_project_item=false
      sub_section=""
      continue
    fi

    # 2-space indent, key:value: Keys under global (e.g., "  base_dir: ~/foo")
    if [[ "$line" =~ ^[[:space:]]{2}([a-z_]+):[[:space:]]*(.*)$ ]]; then
      local key="${BASH_REMATCH[1]}"
      local value="${BASH_REMATCH[2]}"

      # Expand tilde in paths
      if [[ "$value" == ~* ]]; then
        value="${value/#\~/$HOME}"
      fi

      # Reset sub-section for any new 2-space key
      sub_section=""

      if [ "$section" = "global" ]; then
        # Handle global settings
        if [ -z "$value" ]; then
          # It's a list header (e.g., "  copy_files:" or "  protected_branches:")
          in_list=true
          list_type="$key"
        else
          # It's a direct key-value pair
          case "$key" in
            "base_dir")
              CONFIG_WORKTREE_GLOBAL_BASE_DIR="$value"
              ;;
            "open_with")
              CONFIG_DEFAULTS_GLOBAL_OPEN_WITH="$value"
              ;;
            "auto_prune")
              CONFIG_REMOVE_GLOBAL_AUTO_PRUNE="$value"
              ;;
          esac
        fi
      elif [ -z "$value" ]; then
        # Check for hierarchical sub-sections (global: or projects:)
        if [ "$key" = "global" ] || [ "$key" = "projects" ]; then
          in_list=true
          list_type="$key"
          sub_section="$key"
          in_project_item=false
        else
          # Other list header (e.g., defaults:\n  some_list:)
          in_list=true
          list_type="$key"
        fi
      fi
      continue
    fi

    # 2-space indent, dash: Project list items (e.g., "  - name: my-app")
    if [[ "$line" =~ ^[[:space:]]{2}-[[:space:]]*(.*)$ ]]; then
      local rest="${BASH_REMATCH[1]}"

      if [ "$section" = "projects" ]; then
        # Save previous project before starting a new one
        save_project_data

        # Start new project item
        in_project_item=true
        current_project_name=""
        current_base_dir=""
        current_open_with=""
        current_auto_prune=""
        current_copy_files=""
        current_protected_branches=""
        current_hard_protected_branches=""
        in_list=false

        # Check if name: is on the same line as dash
        if [[ "$rest" =~ ^name:[[:space:]]*(.+)$ ]]; then
          current_project_name="${BASH_REMATCH[1]}"
        fi
      fi
      continue
    fi

    # 4-space indent, dash: List items under global (e.g., "    - .editorconfig")
    if [[ "$line" =~ ^[[:space:]]{4}-[[:space:]]*(.*)$ ]]; then
      local rest="${BASH_REMATCH[1]}"

      if [ "$in_list" = true ]; then
        if [ "$section" = "global" ] || [ "$sub_section" = "global" ]; then
          local item="$rest"
          # List item under global (e.g., copy_files or protected_branches)
          case "$list_type" in
            "copy_files")
              CONFIG_WORKTREE_GLOBAL_COPY_FILES="$CONFIG_WORKTREE_GLOBAL_COPY_FILES $item"
              ;;
            "protected_branches")
              CONFIG_PROTECTION_GLOBAL_BRANCHES="$CONFIG_PROTECTION_GLOBAL_BRANCHES $item"
              ;;
            "hard_protected_branches")
              CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES="$CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES $item"
              ;;
          esac
        elif [ "$sub_section" = "projects" ]; then
          # Project list item under hierarchical section
          save_project_data

          # Start new project item
          in_project_item=true
          current_project_name=""
          current_base_dir=""
          current_open_with=""
          current_auto_prune=""
          current_copy_files=""
          current_protected_branches=""
          current_hard_protected_branches=""
          in_list=false

          # Check if name: is on the same line as dash
          if [[ "$rest" =~ ^name:[[:space:]]*(.+)$ ]]; then
            current_project_name="${BASH_REMATCH[1]}"
          fi
        fi
      fi
      continue
    fi

    # 4-space indent, key:value: Project properties (e.g., "    base_dir: ~/custom")
    if [[ "$line" =~ ^[[:space:]]{4}([a-z_]+):[[:space:]]*(.*)$ ]]; then
      local key="${BASH_REMATCH[1]}"
      local value="${BASH_REMATCH[2]}"

      # Expand tilde in paths
      if [[ "$value" == ~* ]]; then
        value="${value/#\~/$HOME}"
      fi

      if [ "$in_project_item" = true ]; then
        if [ -z "$value" ]; then
          # It's a list header under project (e.g., "    copy_files:")
          in_list=true
          list_type="$key"
        else
          # It's a direct value under project (base_dir, open_with, auto_prune)
          case "$key" in
            "base_dir")
              current_base_dir="$value"
              ;;
            "open_with")
              current_open_with="$value"
              ;;
            "auto_prune")
              current_auto_prune="$value"
              ;;
          esac
        fi
      elif [ "$sub_section" = "global" ]; then
        # Global setting under hierarchical section (e.g., defaults:\n  global:\n    open_with: code)
        if [ -z "$value" ]; then
          in_list=true
          list_type="$key"
        else
          case "$key" in
            "base_dir")
              CONFIG_WORKTREE_GLOBAL_BASE_DIR="$value"
              ;;
            "open_with")
              CONFIG_DEFAULTS_GLOBAL_OPEN_WITH="$value"
              ;;
            "auto_prune")
              CONFIG_REMOVE_GLOBAL_AUTO_PRUNE="$value"
              ;;
          esac
        fi
      fi
      continue
    fi

    # 6-space indent, key:value: Project properties under nested projects section
    if [[ "$line" =~ ^[[:space:]]{6}([a-z_]+):[[:space:]]*(.*)$ ]]; then
      local key="${BASH_REMATCH[1]}"
      local value="${BASH_REMATCH[2]}"

      # Expand tilde in paths
      if [[ "$value" == ~* ]]; then
        value="${value/#\~/$HOME}"
      fi

      if [ "$in_project_item" = true ]; then
        if [ -z "$value" ]; then
          # It's a list header under project (e.g., "      copy_files:")
          in_list=true
          list_type="$key"
        else
          # It's a direct value under project
          case "$key" in
            "base_dir")
              current_base_dir="$value"
              ;;
            "open_with")
              current_open_with="$value"
              ;;
            "auto_prune")
              current_auto_prune="$value"
              ;;
          esac
        fi
      fi
      continue
    fi

    # 6-space indent, dash: List items (under project properties or hierarchical global lists)
    if [[ "$line" =~ ^[[:space:]]{6}-[[:space:]]*(.*)$ ]]; then
      local item="${BASH_REMATCH[1]}"

      if [ "$in_project_item" = true ] && [ "$in_list" = true ]; then
        # Add to project's list (comma-separated)
        case "$list_type" in
          "copy_files")
            if [ -z "$current_copy_files" ]; then
              current_copy_files="$item"
            else
              current_copy_files="$current_copy_files,$item"
            fi
            ;;
          "protected_branches")
            if [ -z "$current_protected_branches" ]; then
              current_protected_branches="$item"
            else
              current_protected_branches="$current_protected_branches,$item"
            fi
            ;;
          "hard_protected_branches")
            if [ -z "$current_hard_protected_branches" ]; then
              current_hard_protected_branches="$item"
            else
              current_hard_protected_branches="$current_hard_protected_branches,$item"
            fi
            ;;
        esac
      elif [ "$in_list" = true ] && [ "$sub_section" = "global" ]; then
        # Hierarchical global list item (e.g. protection:\n  global:\n    protected_branches:\n      - dev)
        case "$list_type" in
          "copy_files")
            CONFIG_WORKTREE_GLOBAL_COPY_FILES="$CONFIG_WORKTREE_GLOBAL_COPY_FILES $item"
            ;;
          "protected_branches")
            CONFIG_PROTECTION_GLOBAL_BRANCHES="$CONFIG_PROTECTION_GLOBAL_BRANCHES $item"
            ;;
          "hard_protected_branches")
            CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES="$CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES $item"
            ;;
        esac
      fi
      continue
    fi

    # 8-space indent, dash: List items under nested project properties
    if [[ "$line" =~ ^[[:space:]]{8}-[[:space:]]*(.*)$ ]]; then
      local item="${BASH_REMATCH[1]}"

      if [ "$in_project_item" = true ] && [ "$in_list" = true ]; then
        # Add to project's list (comma-separated)
        case "$list_type" in
          "copy_files")
            if [ -z "$current_copy_files" ]; then
              current_copy_files="$item"
            else
              current_copy_files="$current_copy_files,$item"
            fi
            ;;
          "protected_branches")
            if [ -z "$current_protected_branches" ]; then
              current_protected_branches="$item"
            else
              current_protected_branches="$current_protected_branches,$item"
            fi
            ;;
          "hard_protected_branches")
            if [ -z "$current_hard_protected_branches" ]; then
              current_hard_protected_branches="$item"
            else
              current_hard_protected_branches="$current_hard_protected_branches,$item"
            fi
            ;;
        esac
      fi
      continue
    fi
  done < "$config_file"

  # Save any remaining project data
  save_project_data

  # Trim leading spaces from global lists
  CONFIG_WORKTREE_GLOBAL_COPY_FILES="${CONFIG_WORKTREE_GLOBAL_COPY_FILES## }"
  CONFIG_PROTECTION_GLOBAL_BRANCHES="${CONFIG_PROTECTION_GLOBAL_BRANCHES## }"
  CONFIG_WORKTREE_PROJECTS="${CONFIG_WORKTREE_PROJECTS## }"
  CONFIG_DEFAULTS_PROJECTS="${CONFIG_DEFAULTS_PROJECTS## }"
  CONFIG_PROTECTION_PROJECTS="${CONFIG_PROTECTION_PROJECTS## }"
  CONFIG_PROTECTION_PROJECTS_HARD="${CONFIG_PROTECTION_PROJECTS_HARD## }"
  CONFIG_REMOVE_PROJECTS="${CONFIG_REMOVE_PROJECTS## }"
  CONFIG_WORKTREE_PROJECTS_COPY_FILES="${CONFIG_WORKTREE_PROJECTS_COPY_FILES## }"
}

# Helper function to save accumulated project data
save_project_data() {
  if [ "$in_project_item" = true ] && [ -n "$current_project_name" ]; then
    # Save base_dir if present
    if [ -n "$current_base_dir" ]; then
      CONFIG_WORKTREE_PROJECTS="$CONFIG_WORKTREE_PROJECTS ${current_project_name}:${current_base_dir}"
    fi

    # Save copy_files list if present
    if [ -n "$current_copy_files" ]; then
      CONFIG_WORKTREE_PROJECTS_COPY_FILES="$CONFIG_WORKTREE_PROJECTS_COPY_FILES ${current_project_name}:${current_copy_files}"
    fi

    # Save open_with if present
    if [ -n "$current_open_with" ]; then
      CONFIG_DEFAULTS_PROJECTS="$CONFIG_DEFAULTS_PROJECTS ${current_project_name}:${current_open_with}"
    fi

    # Save protected_branches if present
    if [ -n "$current_protected_branches" ]; then
      CONFIG_PROTECTION_PROJECTS="$CONFIG_PROTECTION_PROJECTS ${current_project_name}:${current_protected_branches}"
    fi

    # Save hard_protected_branches if present
    if [ -n "$current_hard_protected_branches" ]; then
      CONFIG_PROTECTION_PROJECTS_HARD="$CONFIG_PROTECTION_PROJECTS_HARD ${current_project_name}:${current_hard_protected_branches}"
    fi

    # Save auto_prune if present
    if [ -n "$current_auto_prune" ]; then
      CONFIG_REMOVE_PROJECTS="$CONFIG_REMOVE_PROJECTS ${current_project_name}:${current_auto_prune}"
    fi

    # Reset for next project
    current_project_name=""
    current_base_dir=""
    current_open_with=""
    current_auto_prune=""
    current_copy_files=""
    current_protected_branches=""
    current_hard_protected_branches=""
    in_list=false
  fi
  in_project_item=false
}

# Validate configuration values (bash 3.2 compatible)
validate_configuration() {
  # No validation needed for open_with - any command name is accepted
  # Validation happens at runtime when attempting to open

  # Validate worktree.global.base_dir exists or can be created
  if [ -n "$CONFIG_WORKTREE_GLOBAL_BASE_DIR" ]; then
    if [ ! -d "$CONFIG_WORKTREE_GLOBAL_BASE_DIR" ]; then
      local parent_dir=$(dirname "$CONFIG_WORKTREE_GLOBAL_BASE_DIR")
      if [ ! -d "$parent_dir" ]; then
        echo "⚠️  Warning: Configured worktree.global.base_dir parent does not exist: $parent_dir"
        echo "   Falling back to automatic detection"
        CONFIG_WORKTREE_GLOBAL_BASE_DIR=""
      fi
    fi
  fi

  # Validate and normalize remove.global.auto_prune to true/false
  case "$CONFIG_REMOVE_GLOBAL_AUTO_PRUNE" in
    true|false)
      ;;  # Already normalized
    yes|1)
      CONFIG_REMOVE_GLOBAL_AUTO_PRUNE="true"
      ;;
    no|0)
      CONFIG_REMOVE_GLOBAL_AUTO_PRUNE="false"
      ;;
    *)
      echo "⚠️  Warning: Invalid remove.global.auto_prune value '$CONFIG_REMOVE_GLOBAL_AUTO_PRUNE' in config"
      echo "   Valid options: true, false, yes, no, 1, 0"
      echo "   Falling back to default: true"
      CONFIG_REMOVE_GLOBAL_AUTO_PRUNE="true"
      ;;
  esac

  # Validate and normalize project-specific auto_prune values
  local validated_remove_projects=""
  for entry in $CONFIG_REMOVE_PROJECTS; do
    local proj_name="${entry%%:*}"
    local auto_prune="${entry#*:}"
    case "$auto_prune" in
      true|false)
        validated_remove_projects="$validated_remove_projects ${entry}"
        ;;
      yes|1)
        validated_remove_projects="$validated_remove_projects ${proj_name}:true"
        ;;
      no|0)
        validated_remove_projects="$validated_remove_projects ${proj_name}:false"
        ;;
      *)
        echo "⚠️  Warning: Invalid auto_prune value '$auto_prune' for project '$proj_name'"
        echo "   Valid options: true, false, yes, no, 1, 0"
        echo "   This project will use global setting"
        ;;
    esac
  done
  CONFIG_REMOVE_PROJECTS="${validated_remove_projects## }"
}

# Load configuration from file (bash 3.2 compatible)
load_configuration() {
  # Get the directory where git-turnouts is installed
  # Resolve symlinks to find the actual script location
  local script_path="${BASH_SOURCE[0]}"
  while [ -L "$script_path" ]; do
    local link_target=$(readlink "$script_path")
    if [[ "$link_target" == /* ]]; then
      script_path="$link_target"
    else
      script_path="$(dirname "$script_path")/$link_target"
    fi
  done
  local script_dir="$(cd "$(dirname "$script_path")" && pwd)"

  # Allow config file override via environment variable (useful for testing)
  local config_file="${GIT_TURNOUTS_CONFIG:-$script_dir/.config.yml}"

  # Defaults are already set in variable declarations above
  # Only load and parse if config file exists
  if [ -f "$config_file" ]; then
    if [ -r "$config_file" ]; then
      parse_yaml_config "$config_file" 2>/dev/null || {
        echo "⚠️  Warning: Error parsing config file: $config_file"
        echo "   Continuing with defaults..."
      }
      # Validate configuration after loading
      validate_configuration
    else
      echo "⚠️  Warning: Cannot read config file: $config_file"
      echo "   Continuing with defaults..."
    fi
  fi
}

# Get configuration value with fallback (bash 3.2 compatible)
# Supports hierarchical lookup: project-specific → global → hardcoded default
# Usage: get_config "key" "hardcoded_default" ["project_name"]
get_config() {
  local key="$1"
  local default="${2:-}"
  local project="${3:-}"  # Optional: specific project, otherwise auto-detect

  # Auto-detect project if not provided
  if [ -z "$project" ]; then
    project=$(get_project_name 2>/dev/null || echo "")
  fi

  case "$key" in
    # base_dir setting: project-specific → global → auto-detect
    worktree.base_dir)
      # 1. Check project-specific setting
      if [ -n "$project" ]; then
        for entry in $CONFIG_WORKTREE_PROJECTS; do
          local proj_name="${entry%%:*}"
          local proj_path="${entry#*:}"
          if [ "$proj_name" = "$project" ]; then
            echo "$proj_path"
            return
          fi
        done
      fi

      # 2. Check global setting
      if [ -n "$CONFIG_WORKTREE_GLOBAL_BASE_DIR" ]; then
        echo "$CONFIG_WORKTREE_GLOBAL_BASE_DIR"
        return
      fi

      # 3. Use hardcoded default
      echo "$default"
      ;;

    # copy_files setting: project-specific ADDS TO global (additive behavior)
    worktree.copy_files)
      local result=""

      # 1. Start with global setting
      if [ -n "$CONFIG_WORKTREE_GLOBAL_COPY_FILES" ]; then
        result="$CONFIG_WORKTREE_GLOBAL_COPY_FILES"
      fi

      # 2. Add project-specific files (if any)
      if [ -n "$project" ]; then
        for entry in $CONFIG_WORKTREE_PROJECTS_COPY_FILES; do
          local proj_name="${entry%%:*}"
          local proj_files="${entry#*:}"
          if [ "$proj_name" = "$project" ]; then
            # Convert comma-separated to space-separated and append
            local proj_files_space="${proj_files//,/ }"
            if [ -n "$result" ]; then
              result="$result $proj_files_space"
            else
              result="$proj_files_space"
            fi
            break
          fi
        done
      fi

      # 3. Return combined result or default
      if [ -n "$result" ]; then
        echo "$result"
      else
        echo "$default"
      fi
      ;;

    # open_with setting: project-specific → global → none (no automatic opening)
    defaults.open_with)
      # 1. Check project-specific setting
      if [ -n "$project" ]; then
        for entry in $CONFIG_DEFAULTS_PROJECTS; do
          local proj_name="${entry%%:*}"
          local proj_value="${entry#*:}"
          if [ "$proj_name" = "$project" ]; then
            echo "$proj_value"
            return
          fi
        done
      fi

      # 2. Check global setting
      if [ -n "$CONFIG_DEFAULTS_GLOBAL_OPEN_WITH" ]; then
        echo "$CONFIG_DEFAULTS_GLOBAL_OPEN_WITH"
        return
      fi

      # 3. Use hardcoded default
      echo "$default"
      ;;

    # protected_branches setting: project-specific ADDS TO global (additive behavior)
    # Note: main and master are always included as base protection
    protection.protected_branches)
      local result="main master"  # Always start with main and master

      # 1. Add global branches
      if [ -n "$CONFIG_PROTECTION_GLOBAL_BRANCHES" ]; then
        # Global config already includes main/master from initialization
        # Extract only the additional branches (skip main/master to avoid duplicates)
        for branch in $CONFIG_PROTECTION_GLOBAL_BRANCHES; do
          if [ "$branch" != "main" ] && [ "$branch" != "master" ]; then
            result="$result $branch"
          fi
        done
      fi

      # 2. Add project-specific branches (if any)
      if [ -n "$project" ]; then
        for entry in $CONFIG_PROTECTION_PROJECTS; do
          local proj_name="${entry%%:*}"
          local proj_branches="${entry#*:}"
          if [ "$proj_name" = "$project" ]; then
            # Convert comma-separated to space-separated and append
            result="$result ${proj_branches//,/ }"
            break
          fi
        done
      fi

      # 3. Return combined result (or default if somehow empty)
      if [ -n "$result" ]; then
        echo "$result"
      else
        echo "$default"
      fi
      ;;

    # hard_protected_branches setting: project-specific ADDS TO global (additive behavior)
    protection.hard_protected_branches)
      local result=""

      # 1. Add global hard-protected branches
      if [ -n "$CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES" ]; then
        result="$CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES"
      fi

      # 2. Add project-specific hard-protected branches (if any)
      if [ -n "$project" ]; then
        for entry in $CONFIG_PROTECTION_PROJECTS_HARD; do
          local proj_name="${entry%%:*}"
          local proj_branches="${entry#*:}"
          if [ "$proj_name" = "$project" ]; then
            # Convert comma-separated to space-separated and append
            local proj_branches_space="${proj_branches//,/ }"
            if [ -z "$result" ]; then
              result="$proj_branches_space"
            else
              result="$result $proj_branches_space"
            fi
            break
          fi
        done
      fi

      # 3. Return combined result or default
      if [ -n "$result" ]; then
        echo "$result"
      else
        echo "$default"
      fi
      ;;

    # auto_prune setting: project-specific → global → hardcoded default
    remove.auto_prune)
      # 1. Check project-specific setting
      if [ -n "$project" ]; then
        for entry in $CONFIG_REMOVE_PROJECTS; do
          local proj_name="${entry%%:*}"
          local proj_value="${entry#*:}"
          if [ "$proj_name" = "$project" ]; then
            echo "$proj_value"
            return
          fi
        done
      fi

      # 2. Check global setting
      if [ -n "$CONFIG_REMOVE_GLOBAL_AUTO_PRUNE" ]; then
        echo "$CONFIG_REMOVE_GLOBAL_AUTO_PRUNE"
        return
      fi

      # 3. Use hardcoded default
      echo "$default"
      ;;

    # Legacy support for old key formats
    worktree.global.base_dir)
      echo "${CONFIG_WORKTREE_GLOBAL_BASE_DIR:-$default}"
      ;;

    defaults.global.open_with)
      echo "${CONFIG_DEFAULTS_GLOBAL_OPEN_WITH:-$default}"
      ;;

    remove.global.auto_prune)
      echo "${CONFIG_REMOVE_GLOBAL_AUTO_PRUNE:-$default}"
      ;;

    *)
      echo "$default"
      ;;
  esac
}

# Remove a branch from the configuration file
# Arguments: $1 = branch name, $2 = project name (optional)
remove_from_config() {
  local branch="$1"
  local project="${2:-}"

  # Get the config file path
  local script_path="${BASH_SOURCE[0]}"
  while [ -L "$script_path" ]; do
    local link_target=$(readlink "$script_path")
    if [[ "$link_target" == /* ]]; then
      script_path="$link_target"
    else
      script_path="$(dirname "$script_path")/$link_target"
    fi
  done
  local script_dir="$(cd "$(dirname "$script_path")" && pwd)"
  local config_file="${GIT_TURNOUTS_CONFIG:-$script_dir/.config.yml}"

  if [ ! -f "$config_file" ]; then
    return 1
  fi

  # Determine sed -i flag based on OS
  local sed_i_flag=("-i")
  if [[ "$OSTYPE" == "darwin"* ]]; then
    sed_i_flag=("-i" "")
  fi

  if [ -n "$project" ]; then
    # Project-specific removal
    # 1. Find the start line of the project entry
    local start_line=$(grep -n "^  - name: $project" "$config_file" | head -n 1 | cut -d: -f1)
    if [ -z "$start_line" ]; then
      start_line=$(grep -n "^- name: $project" "$config_file" | head -n 1 | cut -d: -f1)
    fi
    
    if [ -z "$start_line" ]; then
      return 1
    fi
    
    # 2. Find the end line of the project entry (next "- name:" or column 0 header)
    local end_line=$(tail -n +$((start_line + 1)) "$config_file" | grep -n -E "^  - name:|^[a-z_]+:" | head -n 1 | cut -d: -f1)
    if [ -n "$end_line" ]; then
        end_line=$((start_line + end_line - 1))
    else
        end_line=$(wc -l < "$config_file")
    fi

    # Remove the branch within that range
    # Indentation for project list items is 6 spaces
    sed "${sed_i_flag[@]}" "${start_line},${end_line}{/^[[:space:]]*-[[:space:]]*${branch}$/d;}" "$config_file"
    echo "✨ Removed '$branch' from protected_branches in project '$project'"
  else
    # Global removal
    local start_line=$(grep -n "^global:" "$config_file" | head -n 1 | cut -d: -f1)
    if [ -z "$start_line" ]; then
      return 1
    fi

    local end_line=$(tail -n +$((start_line + 1)) "$config_file" | grep -n -E "^[a-z_]+:" | head -n 1 | cut -d: -f1)
    if [ -n "$end_line" ]; then
        end_line=$((start_line + end_line - 1))
    else
        end_line=$(wc -l < "$config_file")
    fi

    # Indentation for global list items is 4 spaces
    sed "${sed_i_flag[@]}" "${start_line},${end_line}{/^[[:space:]]*-[[:space:]]*${branch}$/d;}" "$config_file"
    echo "✨ Removed '$branch' from global protected_branches"
  fi
}

# Copy configured files from main worktree to new worktree (bash 3.2 compatible)
copy_configured_files() {
  local source_dir="$1"    # Main worktree path
  local target_dir="$2"    # New worktree path

  # Get copy_files configuration (project-specific → global → empty)
  local copy_files=$(get_config "worktree.copy_files" "")

  # Check if there are files to copy
  if [ -z "$copy_files" ]; then
    return 0
  fi

  echo "📄 Copying configured files..."
  local copied=0
  local skipped=0

  # Iterate over space-separated list
  for file in $copy_files; do
    local source_file="$source_dir/$file"
    local target_file="$target_dir/$file"

    if [ -f "$source_file" ]; then
      # Create target directory if needed
      local target_dir_parent=$(dirname "$target_file")
      mkdir -p "$target_dir_parent"

      # Copy file
      if cp "$source_file" "$target_file" 2>/dev/null; then
        echo "   ✓ Copied: $file"
        copied=$((copied + 1))
      else
        echo "   ⚠ Failed to copy: $file"
        skipped=$((skipped + 1))
      fi
    else
      echo "   ⚠ Not found: $file (skipped)"
      skipped=$((skipped + 1))
    fi
  done

  if [ $copied -gt 0 ]; then
    echo "   Copied $copied file(s)"
  fi
  if [ $skipped -gt 0 ]; then
    echo "   Skipped $skipped file(s)"
  fi
}

# ============================================================================
# ADD COMMAND - Create new worktrees
# ============================================================================

cmd_add() {
  # Load default from config
  local OPEN_APP=$(get_config "defaults.open_with" "")

  # Function to show usage for add command
  show_add_usage() {
    echo "Usage: git-turnouts add <name> [branch-name] [--open <app>]"
    echo ""
    echo "Arguments:"
    echo "  <name>: Folder name for the worktree, PR number, or PR title"
    echo "          If numeric (e.g., 7113), treats as PR number and checkouts that PR"
    echo "          If no branch-name provided, also used as branch name"
    echo "          Will check if this matches any PR title and use that PR's branch"
    echo "  [branch-name]: Optional, specific branch name (defaults to <name>)"
    echo "                 Will check if this matches any PR title and use that PR's branch"
    echo "  --open <app>: Optional, specify where to open (default: none)"
    echo "                Use any command/application that accepts a directory path"
    echo ""
    echo "PR Detection:"
    echo "  - PR Number: If <name> is numeric (e.g., 7113), directly checkouts that PR"
    echo "    Uses PR number as folder name and PR's branch for checkout"
    echo "  - When only <name> is provided: searches open PRs for matching titles"
    echo "    Prefers exact match first, falls back to partial match"
    echo "    If found, uses PR's branch name for both folder and branch"
    echo "  - When both arguments provided: searches open PRs for matching titles"
    echo "    Prefers exact match first, falls back to partial match"
    echo "    If found, uses PR's branch but keeps specified folder name"
    echo "  - Use literal quotes (e.g., '\"Title\"') to force an exact match only"
    echo "  - If no matching PR is found, uses standard branch resolution"
    echo ""
    echo "Examples:"
    echo "  git-turnouts add 7113                         # Checkouts PR #7113"
    echo "  git-turnouts add feature-x                    # Creates feature-x worktree"
    echo "  git-turnouts add JIRA-123                     # Checks for PR with JIRA-123"
    echo "  git-turnouts add \"feature name\"               # Smart title match"
    echo "  git-turnouts add '\"Exact PR Title\"'             # Force exact match"
    echo "  git-turnouts add folder-name branch-name      # Different names"
    echo "  git-turnouts add feature-x --open <editor>    # Opens in specified editor"
  }

  if [ -z "$1" ]; then
    show_add_usage
    exit 1
  fi

  # Parse arguments
  local FOLDER_NAME=""
  local BRANCH=""
  local PARSING_OPEN=false
  local EXTRA_ARGS=0

  for arg in "$@"; do
    if [ "$PARSING_OPEN" = true ]; then
      OPEN_APP="$arg"
      PARSING_OPEN=false
    elif [ "$arg" = "--open" ] || [ "$arg" = "-o" ]; then
      PARSING_OPEN=true
    elif [ -z "$FOLDER_NAME" ]; then
      FOLDER_NAME="$arg"
    elif [ -z "$BRANCH" ]; then
      BRANCH="$arg"
    else
      EXTRA_ARGS=$((EXTRA_ARGS + 1))
    fi
  done

  # Validate argument count
  if [ $EXTRA_ARGS -gt 0 ]; then
    echo "❌ Error: Too many arguments provided"
    echo "Expected: git-turnouts add <name> [branch-name] [--open <app>]"
    echo ""
    echo "You provided more than 2 positional arguments."
    echo "If your argument contains spaces, please enclose it in quotes."
    echo "Example: git-turnouts add \"My PR Title with spaces\""
    echo ""
    show_add_usage
    exit 1
  fi

  # If no branch specified, use folder name
  if [ -z "$BRANCH" ]; then
    BRANCH="$FOLDER_NAME"
  fi

  # Check if we should use a PR branch
  local PR_BRANCH=""
  local ORIGINAL_FOLDER_NAME="$FOLDER_NAME"

  if [ -z "$BRANCH" ] || [ "$BRANCH" = "$FOLDER_NAME" ]; then
    # Case 1: Only <name> argument passed

    # First check if it's a PR number
    if is_pr_number "$FOLDER_NAME"; then
      echo "🔢 Detected PR number: #$FOLDER_NAME"
      echo "🔍 Fetching PR #$FOLDER_NAME details..."

      # Get full PR details including state
      PR_DETAILS=$(get_pr_details_by_number "$FOLDER_NAME")
      if [ -z "$PR_DETAILS" ]; then
        echo "❌ Error: PR #$FOLDER_NAME not found or not accessible"
        exit 1
      fi

      PR_STATE=$(echo "$PR_DETAILS" | jq -r '.state')
      PR_BRANCH=$(echo "$PR_DETAILS" | jq -r '.headRefName')
      BASE_BRANCH=$(echo "$PR_DETAILS" | jq -r '.baseRefName')

      if [ "$PR_STATE" = "MERGED" ]; then
        echo "⚠️  PR #$FOLDER_NAME has been merged into $BASE_BRANCH"
        echo ""
        echo "The PR branch '$PR_BRANCH' may have been deleted from remote."
        echo ""
        echo "Options:"
        echo "  1. Checkout the merge commit from $BASE_BRANCH"
        echo "  2. If the branch still exists remotely, you can try: git fetch origin $PR_BRANCH"
        echo "  3. Create a new feature branch from $BASE_BRANCH"
        exit 1
      elif [ "$PR_STATE" = "CLOSED" ]; then
        echo "⚠️  PR #$FOLDER_NAME is closed (not merged)"
        echo "Branch: $PR_BRANCH"
        echo ""
        echo "Note: The branch may still exist. Proceeding to attempt checkout..."
        BRANCH="$PR_BRANCH"
      else
        echo "📋 Found PR #$FOLDER_NAME (state: $PR_STATE), using branch: $PR_BRANCH"
        BRANCH="$PR_BRANCH"
      fi
    else
      # Not a PR number, check for PR title matching
      set +e  # Temporarily disable exit on error
      SEARCH_TERM=$(check_and_strip_quotes "$FOLDER_NAME")
      WAS_QUOTED=$?
      set -e  # Re-enable exit on error

      if [ $WAS_QUOTED -eq 0 ]; then
        echo "🔍 Checking for PR with exact title '$SEARCH_TERM'..."
        PR_BRANCH=$(get_pr_branch_by_title "$SEARCH_TERM" "true")
        if [ -n "$PR_BRANCH" ]; then
          echo "📋 Found PR with exact title '$SEARCH_TERM', using branch: $PR_BRANCH"
          BRANCH="$PR_BRANCH"
          FOLDER_NAME="${PR_BRANCH//\//-}"
        else
          echo "ℹ️  No PR found with exact title '$SEARCH_TERM', using standard branch resolution"
          FOLDER_NAME="$SEARCH_TERM"
        fi
      else
        echo "🔍 Checking for matching PR title '$SEARCH_TERM'..."
        PR_BRANCH=$(get_pr_branch_by_title "$SEARCH_TERM" "false")
        if [ -n "$PR_BRANCH" ]; then
          echo "📋 Found PR matching title '$SEARCH_TERM', using branch: $PR_BRANCH"
          BRANCH="$PR_BRANCH"
          FOLDER_NAME="${PR_BRANCH//\//-}"
        else
          echo "ℹ️  No PR found matching title '$SEARCH_TERM', using standard branch resolution"
          FOLDER_NAME="$SEARCH_TERM"
        fi
      fi
    fi
  else
    # Case 2: Both <name> and [branch-name] passed
    set +e  # Temporarily disable exit on error
    SEARCH_TERM=$(check_and_strip_quotes "$BRANCH")
    WAS_QUOTED=$?
    set -e  # Re-enable exit on error

    if [ $WAS_QUOTED -eq 0 ]; then
      echo "🔍 Checking for PR with exact title '$SEARCH_TERM'..."
      PR_BRANCH=$(get_pr_branch_by_title "$SEARCH_TERM" "true")
      if [ -n "$PR_BRANCH" ]; then
        echo "📋 Found PR with exact title '$SEARCH_TERM', using branch: $PR_BRANCH"
        BRANCH="$PR_BRANCH"
      else
        echo "ℹ️  No PR found with exact title '$SEARCH_TERM', using standard branch resolution"
        BRANCH="$SEARCH_TERM"
      fi
    else
      echo "🔍 Checking for matching PR title '$SEARCH_TERM'..."
      PR_BRANCH=$(get_pr_branch_by_title "$SEARCH_TERM" "false")
      if [ -n "$PR_BRANCH" ]; then
        echo "📋 Found PR matching title '$SEARCH_TERM', using branch: $PR_BRANCH"
        BRANCH="$PR_BRANCH"
      else
        echo "ℹ️  No PR found matching title '$SEARCH_TERM', using standard branch resolution"
      fi
    fi
  fi

  # No validation needed for OPEN_APP - any command is accepted
  # The command will be validated at runtime when we try to execute it

  # Get the main worktree path
  local MAIN_WORKTREE=$(get_main_worktree)
  local PROJECT_NAME=$(get_project_name)
  local BASE_WORKTREE_DIR=$(get_base_worktree_dir)

  mkdir -p "$BASE_WORKTREE_DIR"

  # Get absolute path of base dir (using portable function instead of realpath)
  local BASE_WORKTREE_DIR_ABS=$(get_absolute_path "$BASE_WORKTREE_DIR")

  # Create project-specific directory (always added for organization)
  local PROJECT_WORKTREE_DIR="$BASE_WORKTREE_DIR_ABS/$PROJECT_NAME"
  mkdir -p "$PROJECT_WORKTREE_DIR"

  # Append folder name
  local TARGET_DIR="$PROJECT_WORKTREE_DIR/$FOLDER_NAME"

  echo "📦 Project: $PROJECT_NAME"
  echo "📁 Worktree folder: $TARGET_DIR"
  echo "🌿 Branch: $BRANCH"

  # Check if target directory already exists
  if [ -d "$TARGET_DIR" ]; then
    echo "❌ Error: Directory already exists: $TARGET_DIR"
    echo "Please choose a different folder name or remove the existing directory first."
    exit 1
  fi

  # Check if branch is already checked out in another worktree
  local EXISTING_WORKTREE=$(git worktree list --porcelain | grep -B2 "branch refs/heads/$BRANCH" | grep "^worktree" | cut -d' ' -f2)
  if [ -n "$EXISTING_WORKTREE" ] && [ "$EXISTING_WORKTREE" != "$TARGET_DIR" ]; then
    echo "❌ Error: Branch '$BRANCH' is already checked out in worktree at: $EXISTING_WORKTREE"
    echo ""
    echo "Options:"
    echo "  1. Use a different folder name to create a new branch"
    echo "  2. Navigate to the existing worktree: cd $EXISTING_WORKTREE"
    echo "  3. Remove the existing worktree: git-turnouts remove $BRANCH"
    exit 1
  fi

  # Check if this was a PR number checkout
  local IS_PR_CHECKOUT=false
  if is_pr_number "$ORIGINAL_FOLDER_NAME" && [ -z "$2" ]; then
    IS_PR_CHECKOUT=true
  fi

  # Create worktree based on branch state
  if [ "$IS_PR_CHECKOUT" = true ]; then
    # For PR checkouts, ensure the PR branch is properly fetched
    echo "🔄 Fetching PR #$ORIGINAL_FOLDER_NAME branch..."

    git fetch origin "$BRANCH" || {
      echo "❌ Error: Failed to fetch PR #$ORIGINAL_FOLDER_NAME branch"
      exit 1
    }

    echo "📦 Creating worktree for PR #$ORIGINAL_FOLDER_NAME (branch: $BRANCH)"
    git worktree add -B "$BRANCH" "$TARGET_DIR" "origin/$BRANCH"
  elif git show-ref --verify --quiet refs/remotes/origin/$BRANCH; then
    # Remote branch exists, create local branch tracking it
    echo "🚀 Creating new local branch '$BRANCH' from 'origin/$BRANCH'"
    git worktree add -B "$BRANCH" "$TARGET_DIR" "origin/$BRANCH"
  else
    # No remote branch, create new branch from HEAD
    echo "🆕 Creating new branch '$BRANCH' from HEAD"
    git worktree add -b "$BRANCH" "$TARGET_DIR" HEAD
  fi

  echo "✅ Worktree created at: $TARGET_DIR (branch: $BRANCH)"

  # Copy configured files
  copy_configured_files "$MAIN_WORKTREE" "$TARGET_DIR"

  # Open in specified application (if configured)
  if [ -n "$OPEN_APP" ]; then
    echo "🚀 Opening with '$OPEN_APP'..."

    # Generic handling: try multiple approaches to support various tools
    local opened=false

    # Approach 1: Try direct execution (works for most CLI tools)
    if command -v "$OPEN_APP" >/dev/null 2>&1; then
      if "$OPEN_APP" "$TARGET_DIR" >/dev/null 2>&1 & then
        echo "✅ Opened with $OPEN_APP"
        opened=true
      fi
    fi

    # Approach 2: Try with 'open -a' for macOS applications (works for GUI apps)
    if [ "$opened" = false ] && command -v open >/dev/null 2>&1; then
      if open -a "$OPEN_APP" "$TARGET_DIR" 2>/dev/null; then
        echo "✅ Opened with $OPEN_APP"
        opened=true
      fi
    fi

    # Show helpful error if all approaches failed
    if [ "$opened" = false ]; then
      echo "⚠️  Warning: Could not open with '$OPEN_APP'"
      echo ""
      echo "   Troubleshooting:"
      if ! command -v "$OPEN_APP" >/dev/null 2>&1; then
        echo "   • Command '$OPEN_APP' not found in PATH"
        echo "   • Make sure it's installed and accessible"
      else
        echo "   • The command exists but may not accept directory paths"
      fi
      echo ""
      echo "   Solutions:"
      echo "   • For CLI tools: Ensure the command accepts a directory path argument"
      echo "   • For macOS apps: The app name should match the application name exactly"
      echo "   • For special cases: Create a wrapper script (e.g., ~/bin/my-opener)"
      echo ""
      echo "   Example wrapper script for custom behavior:"
      echo "   #!/bin/bash"
      echo "   # Save as ~/bin/my-opener and make executable"
      echo "   # Add your custom logic here to open \$1 (the directory path)"
    fi
  else
    # Show helpful tip when no app is opened
    echo ""
    echo "💡 Tip: To automatically open worktrees, use --open <app> or configure open_with in .config.yml"
    echo "   Set in global section or per-project. Use any command that accepts a directory path"
  fi
}

# ============================================================================
# REMOVE COMMAND - Remove worktrees
# ============================================================================

# Function to remove a single worktree
# This is a shared function used by both remove and verify commands
# Arguments: $1 = worktree name, $2 = force (true/false)
remove_worktree() {
  local WORKTREE_NAME="$1"
  local FORCE="${2:-false}"

  # Get all worktrees and find the one matching the given name
  local WORKTREE_PATH=""
  local BRANCH_NAME=""
  while IFS= read -r line; do
    # Extract the branch name from brackets
    if [[ "$line" =~ \[([^\]]+)\] ]]; then
      local BRANCH="${BASH_REMATCH[1]}"
      if [ "$BRANCH" = "$WORKTREE_NAME" ]; then
        # Extract the path (first whitespace-separated token)
        WORKTREE_PATH=$(echo "$line" | awk '{print $1}')
        BRANCH_NAME="$BRANCH"
        break
      fi
    fi
  done < <(git worktree list)

  if [ -z "$WORKTREE_PATH" ]; then
    echo "⚠️  Warning: No worktree found with name '$WORKTREE_NAME'"
    
    # If force is enabled, check if this branch is in the project configuration
    # and remove it if it exists there as a stale entry.
    if [ "$FORCE" = "true" ]; then
      local current_project=$(get_project_name 2>/dev/null || echo "")
      if [ -n "$current_project" ]; then
        # Check if it is in the configuration specifically for this project
        local project_protected=""
        for entry in $CONFIG_PROTECTION_PROJECTS; do
          if [[ "$entry" == "$current_project:"* ]]; then
            project_protected="${entry#*:}"
            project_protected="${project_protected//,/ }"
            break
          fi
        done
        
        for protected in $project_protected; do
          if [ "$WORKTREE_NAME" = "$protected" ]; then
            echo "🛡️  Found '$WORKTREE_NAME' in protected_branches for project '$current_project' (orphaned entry)"
            remove_from_config "$WORKTREE_NAME" "$current_project"
            return 0
          fi
        done
      fi
    fi
    
    return 1
  fi

  echo "Found worktree '$WORKTREE_NAME' at: $WORKTREE_PATH"

  if [ ! -d "$WORKTREE_PATH" ]; then
    echo "⚠️  Warning: Worktree path '$WORKTREE_PATH' does not exist - skipping"
    return 1
  fi

  echo "Removing worktree '$WORKTREE_NAME' at: $WORKTREE_PATH"

  # Check for uncommitted changes
  cd "$WORKTREE_PATH" || {
    echo "⚠️  Warning: Cannot access worktree directory"
    return 1
  }
  if ! git diff --quiet || ! git diff --cached --quiet; then
    echo "⚠️  Warning: Worktree has uncommitted changes"
    echo -n "   Continue with removal? [y/N] "
    read -r response
    if [[ ! "$response" =~ ^[Yy] ]]; then
      echo "   Skipped removal of '$WORKTREE_NAME'"
      cd - > /dev/null
      return 1
    fi
  fi
  cd - > /dev/null

  # Check if this is a hard-protected branch to avoid removing it (never allowed)
  if is_hard_protected_branch "$BRANCH_NAME"; then
    echo "🛡️  Error: Cannot remove hard-protected branch '$BRANCH_NAME'"
    echo "   Critical branches (main, master, and default) are absolutely protected"
    return 1
  fi

  # Check if this is a protected branch (only allowed if force is set)
  if is_protected_branch "$BRANCH_NAME" && [ "$FORCE" != "true" ]; then
    echo "🛡️  Error: Cannot remove protected branch '$BRANCH_NAME'"
    echo "   Protected branches are configured to prevent accidental removal"
    echo "   To remove this worktree, use --force/-f or remove '$BRANCH_NAME' from protected_branches in .config.yml"
    return 1
  fi

  if git worktree remove --force "$WORKTREE_PATH"; then
    echo "✅ Worktree '$WORKTREE_NAME' removed successfully"

    # If this was a protected branch (and we are here because of --force),
    # remove it from the configuration file to keep it in sync
    if is_protected_branch "$BRANCH_NAME"; then
      # Only remove from project-specific config to be safe
      local current_project=$(get_project_name 2>/dev/null || echo "")
      if [ -n "$current_project" ]; then
        remove_from_config "$BRANCH_NAME" "$current_project"
      fi
    fi

    # Now remove the branch
    echo "🗑️  Deleting branch '$BRANCH_NAME'..."
    if git branch -D "$BRANCH_NAME" 2>/dev/null; then
      echo "✅ Branch '$BRANCH_NAME' deleted successfully"
    else
      echo "⚠️  Warning: Could not delete branch '$BRANCH_NAME' (it may not exist locally or be already deleted)"
    fi

    return 0
  else
    echo "❌ Failed to remove worktree '$WORKTREE_NAME'"
    return 1
  fi
}

cmd_remove() {
  show_remove_usage() {
    echo "Usage: git-turnouts remove [options] <worktree-name> [worktree-name2 ...]"
    echo "       git-turnouts rm [options] <worktree-name> [worktree-name2 ...]"
    echo ""
    echo "Options:"
    echo "  -f, --force    Force removal of protected branches (except main/master)"
    echo ""
    echo "Remove one or more worktrees by their branch name."
    echo ""
    echo "Examples:"
    echo "  git-turnouts remove feature-branch"
    echo "  git-turnouts rm -f protected-branch"
    echo "  git-turnouts rm test1 test2 feature-branch"
  }

  if [ $# -eq 0 ]; then
    show_remove_usage
    exit 1
  fi

  local FORCE="false"
  local ARGS=()

  while [ $# -gt 0 ]; do
    case "$1" in
      -f|--force)
        FORCE="true"
        shift
        ;;
      -h|--help)
        show_remove_usage
        exit 0
        ;;
      *)
        ARGS+=("$1")
        shift
        ;;
    esac
  done

  if [ ${#ARGS[@]} -eq 0 ]; then
    show_remove_usage
    exit 1
  fi

  # Track success/failure
  local TOTAL=0
  local REMOVED=0
  local FAILED=0

  # Show appropriate message based on number of worktrees
  if [ ${#ARGS[@]} -eq 1 ]; then
    echo "🗑️  Removing worktree '${ARGS[0]}'..."
  else
    echo "🗑️  Starting bulk worktree removal..."
  fi
  echo

  # Process each worktree name
  for WORKTREE_NAME in "${ARGS[@]}"; do
    TOTAL=$((TOTAL + 1))

    # Show progress counter only for multiple worktrees
    if [ ${#ARGS[@]} -gt 1 ]; then
      echo "[$TOTAL/${#ARGS[@]}] Processing '$WORKTREE_NAME'..."
    fi

    if remove_worktree "$WORKTREE_NAME" "$FORCE"; then
      REMOVED=$((REMOVED + 1))
    else
      FAILED=$((FAILED + 1))
    fi

    echo
  done

  # Prune once at the end (if configured)
  local auto_prune=$(get_config "remove.auto_prune" "true")
  if [ "$auto_prune" = "true" ]; then
    echo "Pruning stale worktree references..."
    git worktree prune
  else
    echo "ℹ️  Skipping prune (disabled in config)"
  fi

  # Summary
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "📊 Summary:"
  echo "   Total: $TOTAL"
  echo "   ✅ Removed: $REMOVED"
  if [ $FAILED -gt 0 ]; then
    echo "   ⚠️  Failed/Skipped: $FAILED"
  fi
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # Show updated list
  echo
  echo "📋 Updated list of worktrees:"
  git worktree list

  # Exit with error if all removals failed
  if [ $REMOVED -eq 0 ] && [ $TOTAL -gt 0 ]; then
    exit 1
  fi
}

# ============================================================================
# LIST COMMAND - List all worktrees
# ============================================================================

cmd_list() {
  echo "📋 Git Worktrees:"
  git worktree list
}

# ============================================================================
# VERIFY COMMAND
# ============================================================================

show_verify_usage() {
  cat << 'EOF'
Usage: git-turnouts verify [options]

Verify worktrees against remote branches and optionally clean up stale ones.

OPTIONS:
    --clean, -c       Remove worktrees with deleted remote branches
    --force, -f       Include protected branches in cleanup (except main/master)
    --dry-run, -n     Preview what would be removed (implies --clean)
    --yes, -y         Skip confirmation prompt
    --verbose, -v     Show detailed status for each worktree
    --help, -h        Show this help message

EXAMPLES:
    git-turnouts verify                    # Check only (safe)
    git-turnouts verify --verbose          # Detailed output
    git-turnouts verify --clean --dry-run  # Preview cleanup
    git-turnouts verify --clean            # Clean with confirmation
    git-turnouts verify --clean --force    # Clean including protected ones
    git-turnouts verify --clean --yes      # Clean without confirmation

EOF
}

handle_verify_cleanup() {
  local dry_run="$1"
  local force_yes="$2"
  local force_removal="${3:-false}"

  echo ""

  # Show protected branches that will be skipped or included
  local protected_stale_count=$(echo "$WORKTREES_PROTECTED_STALE" | wc -w | tr -d ' ')
  local to_remove_protected=""
  local skipped_protected=""

  if [ $protected_stale_count -gt 0 ]; then
    for branch in $WORKTREES_PROTECTED_STALE; do
      if [ "$force_removal" = "true" ] && ! is_hard_protected_branch "$branch"; then
        to_remove_protected="$to_remove_protected $branch"
      else
        skipped_protected="$skipped_protected $branch"
      fi
    done

    # Trim spaces
    to_remove_protected="${to_remove_protected## }"
    skipped_protected="${skipped_protected## }"

    if [ -n "$skipped_protected" ]; then
      echo "🛡️  Protected branches (will be skipped):"
      for branch in $skipped_protected; do
        # Extract path for this branch
        local path=""
        for entry in $WORKTREE_PATHS; do
          if [[ "$entry" == "$branch:"* ]]; then
            path="${entry#*:}"
            break
          fi
        done
        echo "   • $branch"
        echo "     Path: $path"
        if is_hard_protected_branch "$branch"; then
          echo "     Status: Hard-protected (cannot be removed)"
        else
          echo "     Status: Remote deleted but branch is protected (use --force to remove)"
        fi
      done
      echo ""
    fi
  fi

  # Show stale configuration entries
  local stale_config_count=$(echo "$STALE_CONFIG_ENTRIES" | wc -w | tr -d ' ')
  if [ $stale_config_count -gt 0 ]; then
    if [ "$force_removal" = "true" ]; then
      echo "⚙️  Stale configuration entries to remove:"
      for branch in $STALE_CONFIG_ENTRIES; do
        echo "   • $branch (missing locally and on remote)"
      done
      echo ""
    else
      echo "🛡️  Stale configuration entries (will be skipped):"
      for branch in $STALE_CONFIG_ENTRIES; do
        echo "   • $branch"
        echo "     Status: Missing locally and on remote (use --force to remove from config)"
      done
      echo ""
    fi
  fi

  # Show non-protected stale worktrees + force-included protected ones
  local stale_list="$WORKTREES_WITHOUT_REMOTE $to_remove_protected"
  stale_list="${stale_list## }"
  stale_list="${stale_list%% }"

  local stale_count=$(echo "$stale_list" | wc -w | tr -d ' ')
  if [ $stale_count -eq 0 ] && [ $stale_config_count -eq 0 ]; then
    echo "✨ No stale worktrees or configuration entries to remove"
    return 0
  fi

  if [ $stale_count -gt 0 ]; then
    echo "🗑️  Worktrees to remove:"
    
    # Show detailed list with warnings
    for branch in $stale_list; do
      # Extract path for this branch
      local path=""
      for entry in $WORKTREE_PATHS; do
        if [[ "$entry" == "$branch:"* ]]; then
          path="${entry#*:}"
          break
        fi
      done

      local protected_msg=""
      if is_protected_branch "$branch"; then
        protected_msg=" (PROTECTED)"
      fi

      echo "   • ${branch}${protected_msg}"
      echo "     Path: $path"

      # Check for unpushed commits
      if check_unpushed_commits "$branch"; then
        echo "     ⚠️  WARNING: Has $UNPUSHED_COMMIT_COUNT unpushed commit(s)"
      fi
    done
    echo ""
  fi

  # Dry-run mode
  if [ "$dry_run" = "true" ]; then
    echo "🔍 DRY-RUN: No changes will be made"
    echo "   Run without --dry-run to actually remove"
    return 0
  fi

  # Determine if confirmation needed (always ask unless --yes flag)
  local need_confirmation=true
  if [ "$force_yes" = "true" ]; then
    need_confirmation=false
  fi

  # Ask for confirmation
  if [ "$need_confirmation" = "true" ]; then
    local confirm_msg="Remove these worktrees?"
    if [ "$stale_config_count" -gt 0 ] && [ "$force_removal" = "true" ]; then
      confirm_msg="Remove these worktrees and stale config entries?"
    fi
    echo -n "$confirm_msg [y/N] "
    read -r response
    if [[ ! "$response" =~ ^[Yy] ]]; then
      echo "❌ Cleanup cancelled"
      return 1
    fi
  fi

  # Remove worktrees
  if [ $stale_count -gt 0 ]; then
    echo ""
    echo "🗑️  Removing stale worktrees..."

    # Use existing remove_worktree function
    for branch in $stale_list; do
      remove_worktree "$branch" "$force_removal" || true
    done
  fi

  # Clean up stale config entries
  if [ "$force_removal" = "true" ] && [ $stale_config_count -gt 0 ]; then
    echo ""
    echo "⚙️  Cleaning up stale configuration entries..."
    local current_project=$(get_project_name 2>/dev/null || echo "")
    if [ -n "$current_project" ]; then
      for branch in $STALE_CONFIG_ENTRIES; do
        remove_from_config "$branch" "$current_project"
      done
    fi
  fi

  # Auto-prune
  local auto_prune=$(get_config "remove.auto_prune" "true")
  if [ "$auto_prune" = "true" ]; then
    echo ""
    echo "🔧 Pruning stale references..."
    git worktree prune
  fi

  echo ""
  echo "✅ Cleanup complete!"
}

cmd_verify() {
  # Parse flags
  local CLEAN_MODE=false
  local DRY_RUN=false
  local FORCE_YES=false
  local FORCE_REMOVAL=false
  local VERBOSE=false

  for arg in "$@"; do
    case "$arg" in
      --clean|-c)
        CLEAN_MODE=true
        ;;
      --force|-f)
        FORCE_REMOVAL=true
        ;;
      --dry-run|-n)
        DRY_RUN=true
        CLEAN_MODE=true  # Implied
        ;;
      --yes|-y)
        FORCE_YES=true
        ;;
      --verbose|-v)
        VERBOSE=true
        ;;
      --help|-h)
        show_verify_usage
        exit 0
        ;;
      *)
        echo "❌ Error: Unknown option '$arg'"
        show_verify_usage
        exit 1
        ;;
    esac
  done

  # Step 1: Update remote references (always enabled)
  echo "🔄 Updating remote references..."
  if git fetch origin --prune --quiet 2>/dev/null; then
    echo "✅ Remote references updated"
  else
    echo "⚠️  Warning: Failed to fetch remote (continuing with cached data)"
  fi
  echo ""

  # Step 2: Analyze worktrees
  echo "🔍 Checking worktrees against remote..."
  echo ""

  analyze_worktrees_remote_status "$VERBOSE"

  # Step 3: Check for stale configuration entries
  local current_project=$(get_project_name 2>/dev/null || echo "")
  check_stale_config_entries "$current_project"
  local stale_config_count=$(echo "$STALE_CONFIG_ENTRIES" | wc -w | tr -d ' ')

  # Step 4: Display results
  local stale_count=$(echo "$WORKTREES_WITHOUT_REMOTE" | wc -w | tr -d ' ')
  local active_count=$(echo "$WORKTREES_WITH_REMOTE" | wc -w | tr -d ' ')
  local protected_stale_count=$(echo "$WORKTREES_PROTECTED_STALE" | wc -w | tr -d ' ')

  # Always show summary
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "📊 Summary:"
  echo "   ✅ Active: $active_count"
  echo "   ❌ Stale: $stale_count"
  if [ $protected_stale_count -gt 0 ]; then
    echo "   🛡️  Protected (stale): $protected_stale_count"
  fi
  if [ $stale_config_count -gt 0 ]; then
    echo "   ⚙️  Stale Config: $stale_config_count"
  fi
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # Step 5: Handle cleanup or show status message
  if [ "$CLEAN_MODE" = "true" ] && ([ $stale_count -gt 0 ] || ([ $protected_stale_count -gt 0 ] && [ "$FORCE_REMOVAL" = "true" ]) || ([ $stale_config_count -gt 0 ] && [ "$FORCE_REMOVAL" = "true" ])); then
    handle_verify_cleanup "$DRY_RUN" "$FORCE_YES" "$FORCE_REMOVAL"
  elif [ "$CLEAN_MODE" = "true" ] && [ $stale_count -eq 0 ] && ([ $protected_stale_count -gt 0 ] || [ $stale_config_count -gt 0 ]) && [ "$FORCE_REMOVAL" = "false" ]; then
    echo ""
    echo "✨ All stale worktrees are protected or entries are in config - nothing to remove!"
    echo "   Use --force to remove protected worktrees and stale config entries."
  elif [ $stale_count -eq 0 ] && [ $protected_stale_count -eq 0 ] && [ $stale_config_count -eq 0 ]; then
    echo ""
    echo "✨ All worktrees and configuration are up to date!"
  elif [ $stale_count -gt 0 ] || [ $stale_config_count -gt 0 ]; then
    echo ""
    if [ $stale_count -gt 0 ]; then
      echo "💡 Found $stale_count stale worktree(s) with deleted remote branches."
    fi
    if [ $stale_config_count -gt 0 ]; then
      echo "💡 Found $stale_config_count stale entries in project '$current_project' configuration."
    fi
    echo "   Run 'git-turnouts verify --clean' to remove stale worktrees."
    if [ $stale_config_count -gt 0 ] || [ $protected_stale_count -gt 0 ]; then
      echo "   Note: Use --force to also clean up protected worktrees and stale config entries."
    fi
  fi
}

# ============================================================================
# HELP & VERSION
# ============================================================================

show_help() {
  cat << EOF
Git Turnouts v${VERSION} - Your Git branch switching yard

Manage Git worktrees with intelligent GitHub Pull Request integration.
Just like railroad turnouts route trains between tracks, Git Turnouts helps
you seamlessly switch between multiple branches and work contexts.

USAGE:
    git-turnouts <command> [options]

COMMANDS:
    add <name> [branch] [--open <app>]
        Create a new worktree with optional PR integration

    remove [options] <name> [name2 ...]
    rm [options] <name> [name2 ...]
        Remove one or more worktrees (use -f, --force for protected)

    list
    ls
        List all worktrees

    verify [options]
        Check worktrees against remote branches
        Options:
          --clean, -c      Remove worktrees with deleted remote branches
          --force, -f      Include protected branches in cleanup (except main/master)
          --dry-run, -n    Preview what would be removed
          --yes, -y        Skip confirmation prompt
          --verbose, -v    Show detailed status including protection status

    help
    --help
    -h
        Show this help message

    version
    --version
    -v
        Show version information

    config [show|init|check]
        Manage configuration

CONFIGURATION:
    git-turnouts config show
        Display current configuration

    git-turnouts config init
        Create .config.yml from .config.yml.example

    git-turnouts config check [--verbose] [--required] [--optional]
        Check tool dependencies and show status

    Configuration file: .config.yml
    Example template:   .config.yml.example

    See .config.yml.example for all available options:
    - global.base_dir: Custom worktree location
    - global.copy_files: Files to copy to new worktrees
    - global.open_with: Default IDE/editor command (any command that accepts paths)
    - global.protected_branches: Branches that cannot be deleted
    - global.auto_prune: Auto-run git worktree prune after removing
    - projects[].open_with: Per-project overrides (and other settings)

EXAMPLES:
    # Create worktree from PR number
    git-turnouts add 7113

    # Create worktree and open in your editor
    git-turnouts add feature-x --open <editor>

    # Search for PR by title
    git-turnouts add "Add authentication"

    # Remove worktrees
    git-turnouts remove feature-x
    git-turnouts rm feat-1 feat-2 feat-3
    git-turnouts rm --force protected-branch

    # List all worktrees
    git-turnouts list

    # Verify worktrees against remote
    git-turnouts verify
    git-turnouts verify --verbose

    # Clean up stale worktrees
    git-turnouts verify --clean --dry-run
    git-turnouts verify --clean
    git-turnouts verify --clean --force

    # Check tool dependencies
    git-turnouts config check
    git-turnouts config check --verbose

For more information, visit:
https://github.com/andr3van/git-turnouts

EOF
}

show_version() {
  echo "Git Turnouts v${VERSION}"
}

# ============================================================================
# CONFIG COMMAND - Manage configuration
# ============================================================================

cmd_config() {
  local subcommand="${1:-show}"

  # Get the directory where git-turnouts is installed
  # Resolve symlinks to find the actual script location
  local script_path="${BASH_SOURCE[0]}"
  while [ -L "$script_path" ]; do
    local link_target=$(readlink "$script_path")
    if [[ "$link_target" == /* ]]; then
      script_path="$link_target"
    else
      script_path="$(dirname "$script_path")/$link_target"
    fi
  done
  local script_dir="$(cd "$(dirname "$script_path")" && pwd)"

  # Allow config file override via environment variable (useful for testing)
  local config_file="${GIT_TURNOUTS_CONFIG:-$script_dir/.config.yml}"
  local example_file="$script_dir/.config.yml.example"

  case "$subcommand" in
    show)
      echo "📋 Git Turnouts Configuration"
      echo ""
      echo "Configuration file: $config_file"
      [ -f "$config_file" ] && echo "                    (✓ exists)" || echo "                    (not found - using defaults)"
      echo ""
      echo "Example file: $example_file"
      [ -f "$example_file" ] && echo "              (✓ available)" || echo "              (not found)"
      echo ""
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "Current Effective Configuration"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""

      local current_project=$(get_project_name)
      echo "📦 Current Project: $current_project"
      echo ""

      # ========== WORKTREE CONFIGURATION ==========
      echo "🗂️  Worktree Configuration"
      echo "────────────────────────────────────────────────"

      # Base directory
      echo "  Base Directory:"
      if [ -n "$CONFIG_WORKTREE_GLOBAL_BASE_DIR" ]; then
        echo "    Global:  $CONFIG_WORKTREE_GLOBAL_BASE_DIR"
      else
        echo "    Global:  (not configured, using auto-detect)"
      fi

      # Check if current project has override
      local has_project_base=false
      for entry in $CONFIG_WORKTREE_PROJECTS; do
        local proj_name="${entry%%:*}"
        local proj_path="${entry#*:}"
        if [ "$proj_name" = "$current_project" ]; then
          echo "    Project: $proj_path ⭐ ACTIVE OVERRIDE"
          has_project_base=true
          break
        fi
      done

      if [ "$has_project_base" = false ] && [ -n "$CONFIG_WORKTREE_PROJECTS" ]; then
        echo "    Project: (using global setting)"
      fi

      local effective_base=$(get_base_worktree_dir)
      echo "    → Effective: $effective_base/$current_project/[branch]"

      # Copy files
      echo ""
      echo "  Files to Copy:"
      local effective_copy_files=$(get_config "worktree.copy_files" "")
      if [ -n "$CONFIG_WORKTREE_GLOBAL_COPY_FILES" ]; then
        echo "    Global:"
        for file in $CONFIG_WORKTREE_GLOBAL_COPY_FILES; do
          echo "      - $file"
        done
      else
        echo "    Global:  (none)"
      fi

      # Check if current project has override
      local has_project_copy=false
      for entry in $CONFIG_WORKTREE_PROJECTS_COPY_FILES; do
        local proj_name="${entry%%:*}"
        if [ "$proj_name" = "$current_project" ]; then
          echo "    Project: ⭐ ACTIVE OVERRIDE"
          local proj_files="${entry#*:}"
          for file in ${proj_files//,/ }; do
            echo "      - $file"
          done
          has_project_copy=true
          break
        fi
      done

      if [ "$has_project_copy" = false ] && [ -n "$CONFIG_WORKTREE_PROJECTS_COPY_FILES" ]; then
        echo "    Project: (using global setting)"
      fi

      if [ -n "$effective_copy_files" ]; then
        echo "    → Effective: $effective_copy_files"
      else
        echo "    → Effective: (no files to copy)"
      fi

      # All configured projects
      if [ -n "$CONFIG_WORKTREE_PROJECTS" ] || [ -n "$CONFIG_WORKTREE_PROJECTS_COPY_FILES" ]; then
        echo ""
        echo "  All Configured Projects:"
        local all_projects=""
        for entry in $CONFIG_WORKTREE_PROJECTS; do
          local proj_name="${entry%%:*}"
          if [[ ! " $all_projects " =~ " $proj_name " ]]; then
            all_projects="$all_projects $proj_name"
          fi
        done
        for entry in $CONFIG_WORKTREE_PROJECTS_COPY_FILES; do
          local proj_name="${entry%%:*}"
          if [[ ! " $all_projects " =~ " $proj_name " ]]; then
            all_projects="$all_projects $proj_name"
          fi
        done
        for proj in $all_projects; do
          if [ "$proj" = "$current_project" ]; then
            echo "    - $proj ⭐ ACTIVE"
          else
            echo "    - $proj"
          fi
        done
      fi

      # ========== DEFAULTS CONFIGURATION ==========
      echo ""
      echo "⚙️  Default Behavior"
      echo "────────────────────────────────────────────────"
      echo "  Open With (IDE):"
      echo "    Global:  $CONFIG_DEFAULTS_GLOBAL_OPEN_WITH"

      # Check if current project has override
      local has_project_open=false
      for entry in $CONFIG_DEFAULTS_PROJECTS; do
        local proj_name="${entry%%:*}"
        local proj_value="${entry#*:}"
        if [ "$proj_name" = "$current_project" ]; then
          echo "    Project: $proj_value ⭐ ACTIVE OVERRIDE"
          has_project_open=true
          break
        fi
      done

      if [ "$has_project_open" = false ] && [ -n "$CONFIG_DEFAULTS_PROJECTS" ]; then
        echo "    Project: (using global setting)"
      fi

      local effective_open=$(get_config "defaults.open_with" "")
      if [ -n "$effective_open" ]; then
        echo "    → Effective: $effective_open"
      else
        echo "    → Effective: (none - worktrees will not open automatically)"
      fi

      # ========== PROTECTION CONFIGURATION ==========
      echo ""
      echo "🛡️  Branch Protection"
      echo "────────────────────────────────────────────────"
      echo "  Protected Branches:"
      if [ -n "$CONFIG_PROTECTION_GLOBAL_BRANCHES" ]; then
        echo "    Global:"
        for branch in $CONFIG_PROTECTION_GLOBAL_BRANCHES; do
          echo "      - $branch"
        done
      else
        echo "    Global:  main, master (hardcoded defaults)"
      fi

      # Check if current project has override
      local has_project_protect=false
      for entry in $CONFIG_PROTECTION_PROJECTS; do
        local proj_name="${entry%%:*}"
        if [ "$proj_name" = "$current_project" ]; then
          echo "    Project: ⭐ ACTIVE OVERRIDE"
          local proj_branches="${entry#*:}"
          for branch in ${proj_branches//,/ }; do
            echo "      - $branch"
          done
          has_project_protect=true
          break
        fi
      done

      if [ "$has_project_protect" = false ] && [ -n "$CONFIG_PROTECTION_PROJECTS" ]; then
        echo "    Project: (using global setting)"
      fi

      local effective_protect=$(get_config "protection.protected_branches" "main master")
      echo "    → Effective: $effective_protect"
      echo "    Note: main and master are ALWAYS protected"

      echo ""
      echo "  Hard-protected Branches (cannot be removed even with --force):"
      if [ -n "$CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES" ]; then
        echo "    Global:"
        for branch in $CONFIG_PROTECTION_GLOBAL_HARD_BRANCHES; do
          echo "      - $branch"
        done
      else
        echo "    Global:  (none configured)"
      fi

      # Check if current project has hard-protection override
      local has_project_hard_protect=false
      for entry in $CONFIG_PROTECTION_PROJECTS_HARD; do
        local proj_name="${entry%%:*}"
        if [ "$proj_name" = "$current_project" ]; then
          echo "    Project: ⭐ ACTIVE OVERRIDE"
          local proj_branches="${entry#*:}"
          for branch in ${proj_branches//,/ }; do
            echo "      - $branch"
          done
          has_project_hard_protect=true
          break
        fi
      done

      if [ "$has_project_hard_protect" = false ] && [ -n "$CONFIG_PROTECTION_PROJECTS_HARD" ]; then
        echo "    Project: (using global setting)"
      fi

      local effective_hard_protect=$(get_config "protection.hard_protected_branches" "")
      local default_branch=$(git symbolic-ref refs/remotes/origin/HEAD 2>/dev/null | sed 's@^refs/remotes/origin/@@' || echo "main")
      local all_hard="main master $default_branch $effective_hard_protect"
      local unique_hard=""
      for b in $all_hard; do
        if [[ ! " $unique_hard " =~ " $b " ]]; then
          unique_hard="$unique_hard $b"
        fi
      done
      echo "    → Effective:${unique_hard}"
      echo "    Note: main, master, and the default branch are ALWAYS hard-protected"

      # ========== REMOVE CONFIGURATION ==========
      echo ""
      echo "🗑️  Removal Behavior"
      echo "────────────────────────────────────────────────"
      echo "  Auto Prune:"
      echo "    Global:  $CONFIG_REMOVE_GLOBAL_AUTO_PRUNE"

      # Check if current project has override
      local has_project_prune=false
      for entry in $CONFIG_REMOVE_PROJECTS; do
        local proj_name="${entry%%:*}"
        local proj_value="${entry#*:}"
        if [ "$proj_name" = "$current_project" ]; then
          echo "    Project: $proj_value ⭐ ACTIVE OVERRIDE"
          has_project_prune=true
          break
        fi
      done

      if [ "$has_project_prune" = false ] && [ -n "$CONFIG_REMOVE_PROJECTS" ]; then
        echo "    Project: (using global setting)"
      fi

      local effective_prune=$(get_config "remove.auto_prune" "true")
      echo "    → Effective: $effective_prune"

      echo ""
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo "💡 Hierarchy: Project-specific → Global → Hardcoded defaults"
      echo "   Edit: $config_file"
      ;;
    init)
      # Create config file from example
      if [ ! -f "$example_file" ]; then
        echo "❌ Error: Example file not found: $example_file"
        echo "   Please ensure git-turnouts is properly installed"
        exit 1
      fi

      if [ -f "$config_file" ]; then
        echo "⚠️  Config file already exists: $config_file"
        echo "   Remove it first or edit manually"
        exit 1
      fi

      cp "$example_file" "$config_file"
      echo "✅ Created configuration file: $config_file"
      echo "   Edit this file to customize git-turnouts behavior"
      echo ""
      echo "   vim $config_file"
      ;;
    check)
      shift  # Remove 'check' from arguments
      cmd_config_check "$@"
      ;;
    *)
      echo "Usage: git-turnouts config [show|init|check]"
      echo ""
      echo "Commands:"
      echo "  show    Show current configuration (default)"
      echo "  init    Create .config.yml from .config.yml.example"
      echo "  check   Check tool dependencies and show status"
      echo ""
      echo "Examples:"
      echo "  git-turnouts config show"
      echo "  git-turnouts config init"
      echo "  git-turnouts config check"
      ;;
  esac
}

# Helper function to print tool status for config check command
print_tool_status() {
  local tool="$1"
  local verbose="$2"

  if [ "$TOOL_INSTALLED" = true ]; then
    if [ "$verbose" = true ]; then
      printf "  ✅  %-13s %-22s\n" "$tool" "$TOOL_VERSION"
      printf "      Path: %s\n" "$TOOL_PATH"
      printf "      Purpose: %s\n" "$(get_tool_purpose "$tool")"
    else
      printf "  ✅  %-13s %-22s %s\n" "$tool" "$TOOL_VERSION" "$(get_tool_purpose "$tool")"
    fi
  else
    if [ "$verbose" = true ]; then
      printf "  ❌  %-13s %-22s\n" "$tool" "Not installed"
      printf "      Purpose: %s\n" "$(get_tool_purpose "$tool")"
      get_tool_install_instructions "$tool"
    else
      printf "  ❌  %-13s %-22s %s\n" "$tool" "Not installed" "$(get_tool_purpose "$tool")"
    fi
  fi
}

# Show usage for config check command
show_config_check_usage() {
  cat << 'EOF'
Usage: git-turnouts config check [options]

Check status of required and optional tool dependencies.

OPTIONS:
    --verbose, -v     Show detailed information (paths, purposes)
    --required, -r    Show only required tools
    --optional, -o    Show only optional tools
    --help, -h        Show this help message

EXAMPLES:
    git-turnouts config check              # Check all tools
    git-turnouts config check --verbose    # Detailed output
    git-turnouts config check --required   # Only required tools
    git-turnouts config check --optional   # Only optional tools

REQUIRED TOOLS:
    git          Version control and worktree management (requires 2.5+)
    bash         Script execution (requires 3.2+)
    jq           JSON parsing for GitHub PR integration

OPTIONAL TOOLS:
    gh           GitHub Pull Request integration
    shellcheck   Shell script linting for development

EOF
}

# Config check subcommand - check tool dependencies
cmd_config_check() {
  # Parse flags
  local VERBOSE=false
  local REQUIRED_ONLY=false
  local OPTIONAL_ONLY=false

  for arg in "$@"; do
    case "$arg" in
      --verbose|-v)
        VERBOSE=true
        ;;
      --required|-r)
        REQUIRED_ONLY=true
        ;;
      --optional|-o)
        OPTIONAL_ONLY=true
        ;;
      --help|-h)
        show_config_check_usage
        exit 0
        ;;
      *)
        echo "❌ Error: Unknown option '$arg'"
        show_config_check_usage
        exit 1
        ;;
    esac
  done

  # Cannot use both filters
  if [ "$REQUIRED_ONLY" = true ] && [ "$OPTIONAL_ONLY" = true ]; then
    echo "❌ Error: Cannot use both --required and --optional"
    exit 1
  fi

  # Define tool lists
  local required_tools="git bash jq"
  local optional_tools="gh shellcheck"

  # Header
  echo "📋 Tool Dependency Status"
  echo ""

  # Check required tools
  local required_missing=0
  if [ "$OPTIONAL_ONLY" = false ]; then
    echo "Required Tools:"
    if [ "$VERBOSE" = true ]; then
      printf "      %-13s %-22s\n" "Tool" "Version"
      echo "  ─────────────────────────────────────────────────"
    else
      printf "      %-13s %-22s %s\n" "Tool" "Version" "Purpose"
      echo "  ─────────────────────────────────────────────────────────────────────────────────────"
    fi
    for tool in $required_tools; do
      get_tool_info "$tool"
      print_tool_status "$tool" "$VERBOSE"
      if [ "$TOOL_INSTALLED" = false ]; then
        required_missing=$((required_missing + 1))
      fi
    done
    echo ""
  fi

  # Check optional tools
  if [ "$REQUIRED_ONLY" = false ]; then
    echo "Optional Tools:"
    if [ "$VERBOSE" = true ]; then
      printf "      %-13s %-22s\n" "Tool" "Version"
      echo "  ─────────────────────────────────────────────────"
    else
      printf "      %-13s %-22s %s\n" "Tool" "Version" "Purpose"
      echo "  ─────────────────────────────────────────────────────────────────────────────────────"
    fi
    for tool in $optional_tools; do
      get_tool_info "$tool"
      print_tool_status "$tool" "$VERBOSE"
    done
    echo ""
  fi

  # Summary
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  if [ "$REQUIRED_ONLY" = false ]; then
    if [ $required_missing -eq 0 ]; then
      echo "Status: All required tools are installed ✅"
    else
      echo "Status: Missing $required_missing required tool(s) ❌"
      echo ""
      echo "Run the installation commands above to fix missing dependencies."
      exit 1
    fi
  else
    echo "Status: Check complete"
  fi
}

# ============================================================================
# DEPENDENCY CHECKS
# ============================================================================

# Helper function to get version string for a tool
get_tool_version() {
  local tool="$1"
  case "$tool" in
    git)
      git --version 2>/dev/null | sed 's/git version //' | awk '{print $1}'
      ;;
    jq)
      jq --version 2>/dev/null | sed 's/jq-//'
      ;;
    gh)
      gh --version 2>/dev/null | head -1 | sed 's/gh version //' | awk '{print $1}'
      ;;
    bash)
      bash --version 2>/dev/null | head -1 | sed 's/.*version //' | awk '{print $1}'
      ;;
    shellcheck)
      shellcheck --version 2>/dev/null | grep '^version:' | awk '{print $2}'
      ;;
    *)
      echo "unknown"
      ;;
  esac
}

# Helper function to get full path to a tool
get_tool_path() {
  local tool="$1"
  command -v "$tool" 2>/dev/null || echo ""
}

# Helper function to check if a tool is installed
check_tool_installed() {
  local tool="$1"
  command -v "$tool" >/dev/null 2>&1
}

# Helper function to get all info about a tool
# Sets global variables: TOOL_INSTALLED, TOOL_VERSION, TOOL_PATH
get_tool_info() {
  local tool="$1"

  if check_tool_installed "$tool"; then
    TOOL_INSTALLED="true"
    TOOL_VERSION=$(get_tool_version "$tool")
    TOOL_PATH=$(get_tool_path "$tool")
  else
    TOOL_INSTALLED="false"
    TOOL_VERSION=""
    TOOL_PATH=""
  fi
}

# Helper function to get human-readable description of a tool
get_tool_purpose() {
  local tool="$1"
  case "$tool" in
    git)
      echo "Version control and worktree management"
      ;;
    jq)
      echo "JSON parsing for GitHub PR integration"
      ;;
    bash)
      echo "Script execution (requires 3.2+)"
      ;;
    gh)
      echo "GitHub Pull Request integration"
      ;;
    shellcheck)
      echo "Shell script linting for development"
      ;;
    *)
      echo "Unknown tool"
      ;;
  esac
}

# Helper function to get installation instructions for a tool
get_tool_install_instructions() {
  local tool="$1"
  case "$tool" in
    git)
      echo "  Install: https://git-scm.com/downloads"
      echo "  macOS:   brew install git"
      echo "  Linux:   apt-get install git or yum install git"
      ;;
    jq)
      echo "  Install: https://jqlang.github.io/jq/download/"
      echo "  macOS:   brew install jq"
      echo "  Linux:   apt-get install jq or yum install jq"
      ;;
    gh)
      echo "  Install: https://cli.github.com/"
      echo "  macOS:   brew install gh"
      echo "  Linux:   See https://github.com/cli/cli/blob/trunk/docs/install_linux.md"
      ;;
    shellcheck)
      echo "  Install: https://github.com/koalaman/shellcheck#installing"
      echo "  macOS:   brew install shellcheck"
      ;;
    bash)
      echo "  Update:  Your bash version should work, but for the latest:"
      echo "  macOS:   brew install bash"
      ;;
    *)
      echo "  No installation instructions available"
      ;;
  esac
}

check_dependencies() {
  local missing=""
  local has_error=false

  # Check for required commands
  if ! command -v git >/dev/null 2>&1; then
    missing="${missing}git "
    has_error=true
  fi

  if ! command -v jq >/dev/null 2>&1; then
    missing="${missing}jq "
    has_error=true
  fi

  # Check for optional but important commands
  if ! command -v gh >/dev/null 2>&1; then
    echo "⚠️  Warning: GitHub CLI (gh) not found - PR integration will not work"
    echo "   Install from: https://cli.github.com/"
    echo ""
  fi

  # Check git version supports worktrees
  if command -v git >/dev/null 2>&1; then
    if ! git worktree list >/dev/null 2>&1; then
      echo "❌ Error: Your git version doesn't support worktrees"
      echo "   Please upgrade to git 2.5 or newer"
      echo "   Current version: $(git --version)"
      exit 1
    fi
  fi

  # Report missing required dependencies
  if [ "$has_error" = true ]; then
    echo "❌ Error: Missing required dependencies: $missing"
    echo ""
    echo "Please install the missing tools:"
    if [[ "$missing" == *"git"* ]]; then
      echo "  • git: https://git-scm.com/downloads"
    fi
    if [[ "$missing" == *"jq"* ]]; then
      echo "  • jq: https://jqlang.github.io/jq/download/"
      echo "    - macOS: brew install jq"
      echo "    - Linux: apt-get install jq or yum install jq"
    fi
    exit 1
  fi
}

# ============================================================================
# MAIN DISPATCHER
# ============================================================================

# Check if we're in a git repository
if ! git rev-parse --git-dir > /dev/null 2>&1; then
  echo "❌ Error: Not a git repository"
  exit 1
fi

# Load configuration
load_configuration

# Check dependencies
check_dependencies

# Parse command
COMMAND="${1:-help}"
shift || true

case "$COMMAND" in
  add)
    cmd_add "$@"
    ;;
  remove|rm)
    cmd_remove "$@"
    ;;
  list|ls)
    cmd_list "$@"
    ;;
  verify)
    cmd_verify "$@"
    ;;
  config)
    cmd_config "$@"
    ;;
  help|--help|-h)
    show_help
    ;;
  version|--version|-v)
    show_version
    ;;
  *)
    echo "❌ Error: Unknown command '$COMMAND'"
    echo ""
    echo "Run 'git-turnouts help' for usage information"
    exit 1
    ;;
esac
